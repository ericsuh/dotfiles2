# Tools

Prefer the following CLI tools over older "standard" tools:

- `rg` instead of `grep`
- `fd` instead of `find`
- `uv` instead of `pip`/`pip3`
- `pnpm` instead of `npm`
- `mise` instead of `direnv`, `pyenv`, `nodenv`, `nvm`, etc.
- `turbo` instead of `lerna`

