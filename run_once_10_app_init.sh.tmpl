#!/usr/bin/env bash

set -euo pipefail

git lfs install
{{ if eq .chezmoi.os "darwin" -}}
$(brew --prefix)/opt/fzf/install --no-update-rc --key-bindings --completion
{{ end -}}
echo ":PlugInstall" | nvim --headless -s - +q
